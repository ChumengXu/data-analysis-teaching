# Writing a function

# 1) Write a function to take the sum of 2 integers
# 2) Add a default argument to your function for one of the integers
# 3) Add an optional argument called "verbose" to your function that prints: 
#     "Calculating the sum for 2 integers; hang tight!" (even better if you include the 2 integers)

SumTwoInt <- function(x,y) {
  z <- x + y
  print(z)
  z
}
